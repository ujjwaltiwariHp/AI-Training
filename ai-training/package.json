{
  "name": "ai-training",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev:day0": "turbo run dev --filter=day0-foundation"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}
